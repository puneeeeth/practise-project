name: Issue Number Example
on:
  issues:
    types: 
      - opened
      - edited
      - closed
jobs:
  Output_job:
    runs-on: ubuntu-latest
    steps:
      - name: Output the execution
        run: echo "Here is the output ${{ toJSON(github)}}"